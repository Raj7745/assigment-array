#include<stdio.h>
#include<conio.h>   // create array of 7 elements  with null (0) inisiliztion display all element
int main()
{

    int arr[7]={0};

    printf(" array  element is %d ",arr[0]);
    printf(" array  element is %d ",arr[1]);
    printf(" array  element is %d ",arr[2]);
    printf(" array  element is %d ",arr[3]);
    printf(" array  element is %d ",arr[4]);
    printf(" array  element is %d ",arr[5]);
    printf(" array  element is %d ",arr[6]);






    return 0;
}